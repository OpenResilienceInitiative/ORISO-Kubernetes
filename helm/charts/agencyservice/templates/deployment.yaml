apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agencyservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "agencyservice.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "agencyservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agencyservice.selectorLabels" . | nindent 8 }}
        tier: backend
    spec:
      containers:
      - name: agencyservice
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8084
          protocol: TCP
        # Load all configuration from ConfigMaps and Secrets
        envFrom:
        - configMapRef:
            name: {{ include "agencyservice.fullname" . }}-config
        - secretRef:
            name: {{ include "agencyservice.fullname" . }}-secrets
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          {{- toYaml .Values.livenessProbe | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.readinessProbe | nindent 10 }}

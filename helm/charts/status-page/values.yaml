# Status Page Helm Chart - Values
# All values from ACTUAL running cluster resources

# Application Configuration
image:
  repository: ghcr.io/openresilienceinitiative/oriso-status
  tag: rebuild
  pullPolicy: Never  # Use local image from k3s

# Deployment Configuration
replicas: 1
namespace: ""

resources: {}

# Service Configuration
service:
  type: LoadBalancer
  port: 9200
  targetPort: 9200

# Environment Variables
env:
  PORT: "9200"


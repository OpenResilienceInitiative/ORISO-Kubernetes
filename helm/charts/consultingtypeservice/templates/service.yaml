apiVersion: v1
kind: Service
metadata:
  name: {{ include "consultingtypeservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "consultingtypeservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "consultingtypeservice.selectorLabels" . | nindent 4 }}



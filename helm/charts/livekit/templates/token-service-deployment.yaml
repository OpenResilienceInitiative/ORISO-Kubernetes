{{- $global := .Values.global | default dict -}}
{{- $domains := get $global "domains" | default dict -}}
{{- $livekitDomain := get $domains "livekit" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "livekit.fullname" . }}-token-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "livekit.token.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.tokenService.replicas }}
  selector:
    matchLabels:
      app: livekit-token-service
  template:
    metadata:
      labels:
        app: livekit-token-service
    spec:
      containers:
      - name: livekit-token-service
        image: "{{ .Values.tokenService.image.repository }}:{{ .Values.tokenService.image.tag }}"
        imagePullPolicy: {{ .Values.tokenService.image.pullPolicy }}
        env:
        - name: LIVEKIT_URL
          value: "wss://{{ required "global.domains.livekit is required for the LiveKit token service URL" $livekitDomain }}"
        ports:
        - name: http
          containerPort: {{ .Values.tokenService.service.targetPort }}
          protocol: TCP
        {{- if .Values.resources }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        {{- end }}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      terminationGracePeriodSeconds: 30

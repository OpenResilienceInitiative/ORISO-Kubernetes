apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "livekit.fullname" . }}-config
  namespace: {{ default .Release.Namespace .Values.namespace }}
data:
  livekit.yaml: |
{{ .Values.livekit.config | indent 4 }}


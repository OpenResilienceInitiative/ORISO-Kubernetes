apiVersion: v1
kind: Service
metadata:
  name: element-call
  namespace: {{ .Release.Namespace }}
  labels:
    app: element-call
spec:
  type: ClusterIP
  selector:
    app: element-call
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
